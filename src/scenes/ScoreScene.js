class ScoreScene extends Phaser.Scene {
    constructor(test) {
        super({
            key: 'ScoreScene'
        });
    }
    preload() {
       
    }
    create() {
       
    }

    update(time, delta) {
        
    }
}

export default ScoreScene;
